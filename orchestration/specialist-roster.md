# Specialist Roster

*First-session mandate item 3. Created 2026-05-26. The Code Orchestrator briefs specialists; it does not spawn them. Each entry is what's needed to write a sound brief and to know when a specialist is straying.*

## Entry schema
`name · mandate · scope · load-bearing inputs · launch-prompt (artifact id or GAP) · status · escalation-back triggers`

## Infrastructure lane (Code Orchestrator coordinates)

### SIGMA Architect
- **Mandate:** design and stress-test the formal axiom layer (SIGMA — the constitutional layer above ECOS).
- **Scope:** SIGMA atoms/worldviews, naming protocol application, primitive classification. Not ECOS operations.
- **Load-bearing inputs:** SIGMA Architect launch prompt — **GAP: not in ECB** (consistent with inherited open loop #7). SIGMA content artifacts exist: `d911f2d5` (Definition & Articulation), `34863a0e` (Naming Protocol).
- **Launch prompt:** **GAP** — must be captured before a clean SIGMA session.
- **Status:** active open loops — two-door-principle reclassification (BRAIN primitive vs SIGMA atom), potential legibility-axiom, trinity test (ground/orient/constitute) against a **SIGMA-internal or hypothetical** worked example — **not TTC**. *(TTC is the downstream first-real-world usage test, run in a separate session; strictly out of the SIGMA sprint per Levi 2026-05-26.)*
- **Escalate back if:** a SIGMA change would alter an ECOS operational invariant; a primitive's classification is contested.

### TTC Architecture Designer
- **Mandate:** TTC workspace architecture design.
- **Scope:** workspace structure and surface-fit; not TTC governance/legal strategy (that's the legal/comms lane below).
- **Load-bearing inputs:** workspace architecture rubric + surface-fit mapping — **GAP: not in ECB**.
- **Launch prompt:** **GAP**.
- **Status:** queued.
- **Escalate back if:** architecture choices imply governance/legal posture; surface-fit conflicts with held TTC state.

### Per-workstream prep agents
- **Mandate:** bounded prep per workstream.
- **Load-bearing input:** TTC workstream enumeration — pending; the **Babysitter surfaces this as its first ask** (not the orchestrator's to derive cold).
- **Status:** downstream.

## The orchestration/babysitter pair (the coordinating pair itself — not a specialist)
The Grand Orchestration Engine (`65ab2668`) + Babysitter Sub-Agent (`ecdc2117`) are **not a separate lane**. They are the **TTC-email instance** of the same customizable-core orchestration/babysitter pair defined in `orchestration-contract.md` → *Pair Pattern & Instances*. The Code Orchestrator + Babysitter (Claude.ai Project) are the **infrastructure instance** of that same core.

**Reuse the core; configure per surface/domain — don't re-derive.** The infrastructure instance was re-derived on 2026-05-26 only because the prior core wasn't captured/compounded/propagated. Before standing up any future orchestration pair, instantiate from the contract's core rather than rebuilding it.

## Gaps to close (carried into STATE.md drift/decision queues)
- Capture the **SIGMA Architect launch prompt** as an ECB artifact before the next SIGMA session.
- Capture the **TTC workspace rubric + surface-fit mapping** before the TTC Architecture Designer launches.
