# macOS
.DS_Store

# Credentials — never commit these
.env
.env.*
!.env.example
!.env.*.example
.claude/rules/supabase.env
.claude/settings.local.json

# Machine-specific symlinks (point to local Obsidian vault path)
.claude/skills

# Supabase local dev artifacts
supabase/.temp/
supabase/.branches/

# Deno lockfile (regenerated by `deno check`; not pinned for edge functions here)
supabase/functions/ecb-mcp/deno.lock

# Playwright driver screenshots (run-crm-dashboard skill output)
shots/

# Other-agent local runtime (Codex, etc.) — not repo content.
# Canonical agent context lives in CLAUDE.md; AGENTS.md here is a generated duplicate.
.codex/
.agents/
AGENTS.md
.vercel
.env*.local
